// Base URLs, environment constants, shared config

// Language Menu Items
export interface MenuItemType {
  label: string;
}

export const languageMenu: MenuItemType[] = [
  { label: "Global (English)" },
  { label: "France (Français)" },
];

// Footer Config
export const footerHeight = 75;

